/**
 * 📝 Notes for Contributors:
 */

import { BlockPresenter } from "@learnblocks/utils"
import { withAtoms } from "@learnblocks/utils"
import * as React from "react"
import { {{properCase componentName}}Block } from "./types"

export const {{properCase componentName}}PresenterComponent: BlockPresenter<{{properCase componentName}}Block> = ({
  block,
  onResult,
}) => {
  React.useEffect(() => onResult && onResult({}), [onResult, block.text])

  return <div>{block.text}</div>
}

export const {{properCase componentName}}Presenter = withAtoms({{properCase componentName}}PresenterComponent)
