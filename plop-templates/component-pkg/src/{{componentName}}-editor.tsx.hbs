/**
 * 📝 Notes for Contributors:
 */

import * as React from "react"

export interface {{capitalize componentName}}EditorProps {
}

export function {{capitalize componentName}}Editor(props: {{capitalize componentName}}EditorProps) {
  return <div/>;
}